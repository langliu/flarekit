{
  "name": "server",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "build": "vp pack",
    "check-types": "tsc -b",
    "compile": "bun build --compile --minify --sourcemap --bytecode ./src/index.ts --outfile server"
  },
  "dependencies": {
    "@flarekit/api": "workspace:*",
    "@flarekit/auth": "workspace:*",
    "@flarekit/db": "workspace:*",
    "@flarekit/env": "workspace:*",
    "@orpc/openapi": "catalog:",
    "@orpc/server": "catalog:",
    "@orpc/zod": "catalog:",
    "better-auth": "catalog:",
    "dotenv": "catalog:",
    "hono": "catalog:",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "catalog:",
    "@flarekit/config": "workspace:*",
    "@types/node": "catalog:",
    "alchemy": "catalog:",
    "typescript": "catalog:",
    "wrangler": "catalog:"
  }
}
